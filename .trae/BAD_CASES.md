## 2026-04-06 入口文件命名错误

### 问题现象
- 执行 `npm run dev` 时，Node 报错 `MODULE_NOT_FOUND`
- 页面入口与脚本启动都引用 `main.js`，但实际文件名写成了 `mian.js`

### 违规根因
- 入口文件命名与引用不一致，导致运行时无法解析模块
- 浏览器入口和脚本入口缺少统一校验，低级拼写错误直接进入运行阶段

### 修正方案
- 将入口文件统一为 `src/main.js`
- 同步修正 `index.html` 中的模块引用
- 删除误拼写的旧文件，避免后续再次误用
